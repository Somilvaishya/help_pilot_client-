[project]
name = "help_pilot_client"
authors = [
    { name = "Somil Vaishya", email = "somil@example.com" }
]
description = "Raise Help Pilot tickets from any ERP site, without leaving it"
requires-python = ">=3.10"
readme = "README.md"
dynamic = ["version"]
dependencies = []

[build-system]
requires = ["flit_core >=3.4,<4"]
build-backend = "flit_core.buildapi"

[tool.bench.frappe-dependencies]
frappe = ">=15.0.0,<16.0.0"
